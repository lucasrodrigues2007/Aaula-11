function App() {
    // Isto parece HTML, mas é JSX!
    return (
      <div className="container">
        <h1>Olá, Mundo com React e JSX!</h1>
        <p>Meu primeiro componente React.</p>
      </div>
    );
    // Nota: usamos className em vez de class para CSS.
  }

  

